/**
 * The canonical origin for the site. This is the single source of truth —
 * import it, never re-declare it.
 *
 * It must match the domain Vercel actually serves on, or every canonical tag,
 * sitemap URL, and JSON-LD `url` will point at a host that redirects. Vercel
 * currently serves `www` and redirects the apex to it.
 */
export const SITE_URL = "https://www.sactabernacle.com";

export const SITE_NAME = "Sacramento Tabernacle";
